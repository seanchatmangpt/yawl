# ZAI integration test environment (source before running run-integration-tests.sh)
# Copy to zai-integration.env and set ZAI_API_KEY, then: source scripts/zai-integration.env

export YAWL_ENGINE_URL="${YAWL_ENGINE_URL:-http://localhost:8080/yawl}"
export YAWL_USERNAME="${YAWL_USERNAME:-admin}"
export YAWL_PASSWORD="${YAWL_PASSWORD:-YAWL}"
export MCP_SERVER_URL="${MCP_SERVER_URL:-http://localhost:3000}"
export A2A_AGENT_URL="${A2A_AGENT_URL:-http://localhost:8081}"
# Required for ZAI checks (SelfPlayTest exits without it):
# export ZAI_API_KEY="your-api-key"
# Optional: override model if API returns "Unknown Model" (e.g. glm-4.6, glm-5, GLM-4.7-Flash):
# export ZAI_MODEL="glm-4.6"
