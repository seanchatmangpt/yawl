---
name: cancel-ralph
description: Cancel an active ralph-loop session
---

Cancel an active ralph-loop session.

**Usage**: `/cancel-ralph`

This clears the loop state and allows the session to exit normally.

Execute: `bash .claude/skills/cancel-ralph.sh`
