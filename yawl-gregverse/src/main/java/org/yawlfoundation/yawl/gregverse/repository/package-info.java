/**
 * Spring Data JPA repositories for YAWL Gregverse application.
 *
 * <p>This package contains repository interfaces that provide data access
 * operations for workflow entities using Spring Data JPA.</p>
 *
 * <h2>Key Repositories</h2>
 * <ul>
 *   <li>{@code WorkflowDefinitionRepository} - CRUD for workflow definitions</li>
 *   <li>{@code WorkflowInstanceRepository} - Query and update workflow instances</li>
 *   <li>{@code TaskExecutionRepository} - Task execution history access</li>
 *   <li>{@code VariableBindingRepository} - Variable value persistence</li>
 * </ul>
 *
 * @since 6.0.0
 */
package org.yawlfoundation.yawl.gregverse.repository;
