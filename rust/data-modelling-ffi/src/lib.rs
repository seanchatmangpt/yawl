// @generated by ggen from dm-bridge.ttl (WCP-NB-09)
// data-modelling-sdk v2.3.0 — C ABI cdylib
// Pattern: Native Bridge WCP-NB-09
// DO NOT EDIT — regenerate with: ggen sync dm-bridge-ggen/

#![allow(clippy::not_unsafe_ptr_arg_deref)]

use std::ffi::{c_char, CStr, CString};

mod dm_sdk {
    use super::*;

    fn err(msg: &str) -> String {
        format!(r#"{{"error":"sdk_error","message":"{}"}}"#, msg.replace('"', "\\\""))
    }

    fn escape(s: &str) -> String {
        s.replace('\\', "\\\\").replace('"', "\\\"")
    }

    pub fn import_odcs(input: &str) -> String {
        match serde_json::from_str::<serde_json::Value>(input) {
            Ok(val) => {
                let content = val.get("content").and_then(|v| v.as_str()).unwrap_or(input);
                let mut imp = data_modelling_sdk::import::ODCSImporter::new();
                match imp.import(content) {
                    Ok(result) => {
                        let schema_id = uuid::Uuid::new_v4().to_string();
                        let name = result.tables.first()
                            .and_then(|t| t.name.as_deref())
                            .unwrap_or("unknown");
                        let tables = result.tables.len();
                        format!(r#"{{"schema_id":"{}","vocabulary_id":"{}","name":"{}","tables":{}}}"#,
                            schema_id, schema_id, escape(name), tables)
                    }
                    Err(e) => err(&format!("ODCS import failed: {}", e)),
                }
            }
            Err(e) => err(&format!("JSON parse error: {}", e)),
        }
    }

    pub fn import_odps(input: &str) -> String {
        match serde_json::from_str::<serde_json::Value>(input) {
            Ok(val) => {
                let content = val.get("content").and_then(|v| v.as_str()).unwrap_or(input);
                match data_modelling_sdk::import::ODPSImporter::new().import(content) {
                    Ok(result) => serde_json::to_string(&result)
                        .unwrap_or_else(|e| err(&format!("Serialization error: {}", e))),
                    Err(e) => err(&format!("ODPS import failed: {}", e)),
                }
            }
            Err(e) => err(&format!("JSON parse error: {}", e)),
        }
    }

    pub fn import_cads(input: &str) -> String {
        match serde_json::from_str::<serde_json::Value>(input) {
            Ok(val) => {
                let content = val.get("content").and_then(|v| v.as_str()).unwrap_or(input);
                match data_modelling_sdk::import::CADSImporter::new().import(content) {
                    Ok(result) => serde_json::to_string(&result)
                        .unwrap_or_else(|e| err(&format!("Serialization error: {}", e))),
                    Err(e) => err(&format!("CADS import failed: {}", e)),
                }
            }
            Err(e) => err(&format!("JSON parse error: {}", e)),
        }
    }

    pub(super) fn parse_domain_id(val: &serde_json::Value) -> uuid::Uuid {
        val.get("domain_id")
            .and_then(|v| v.as_str())
            .and_then(|s| uuid::Uuid::parse_str(s).ok())
            .unwrap_or_else(|| uuid::Uuid::parse_str("00000000-0000-0000-0000-000000000001").unwrap())
    }

    pub fn import_openapi(input: &str) -> String {
        match serde_json::from_str::<serde_json::Value>(input) {
            Ok(val) => {
                let content = val.get("content").and_then(|v| v.as_str()).unwrap_or(input);
                let domain_uuid = parse_domain_id(&val);
                let mut imp = data_modelling_sdk::import::openapi::OpenAPIImporter::new();
                match imp.import(content, domain_uuid, None) {
                    Ok(result) => serde_json::to_string(&result)
                        .unwrap_or_else(|e| err(&format!("Serialization error: {}", e))),
                    Err(e) => err(&format!("OpenAPI import failed: {}", e)),
                }
            }
            Err(e) => err(&format!("JSON parse error: {}", e)),
        }
    }

    pub fn import_bpmn(input: &str) -> String {
        match serde_json::from_str::<serde_json::Value>(input) {
            Ok(val) => {
                let content = val.get("content").and_then(|v| v.as_str()).unwrap_or(input);
                let domain_uuid = parse_domain_id(&val);
                let mut imp = data_modelling_sdk::import::bpmn::BPMNImporter::new();
                match imp.import(content, domain_uuid, None) {
                    Ok(result) => {
                        let process_id = result.id.to_string();
                        let name = escape(&result.name);
                        format!(r#"{{"process_id":"{}","name":"{}","domain_id":"{}"}}"#,
                            process_id, name, result.domain_id)
                    }
                    Err(e) => err(&format!("BPMN import failed: {}", e)),
                }
            }
            Err(e) => err(&format!("JSON parse error: {}", e)),
        }
    }

    pub fn import_dmn(input: &str) -> String {
        match serde_json::from_str::<serde_json::Value>(input) {
            Ok(val) => {
                let content = val.get("content").and_then(|v| v.as_str()).unwrap_or(input);
                let domain_uuid = parse_domain_id(&val);
                let mut imp = data_modelling_sdk::import::dmn::DMNImporter::new();
                match imp.import(content, domain_uuid, None) {
                    Ok(result) => serde_json::to_string(&result)
                        .unwrap_or_else(|e| err(&format!("Serialization error: {}", e))),
                    Err(e) => err(&format!("DMN import failed: {}", e)),
                }
            }
            Err(e) => err(&format!("JSON parse error: {}", e)),
        }
    }

    pub fn import_decision(input: &str) -> String {
        match serde_json::from_str::<serde_json::Value>(input) {
            Ok(val) => {
                let content = val.get("content").and_then(|v| v.as_str()).unwrap_or(input);
                match data_modelling_sdk::import::DecisionImporter::new().import(content) {
                    Ok(result) => serde_json::to_string(&result)
                        .unwrap_or_else(|e| err(&format!("Serialization error: {}", e))),
                    Err(e) => err(&format!("Decision import failed: {}", e)),
                }
            }
            Err(e) => err(&format!("JSON parse error: {}", e)),
        }
    }

    pub fn import_knowledge(input: &str) -> String {
        match serde_json::from_str::<serde_json::Value>(input) {
            Ok(val) => {
                let content = val.get("content").and_then(|v| v.as_str()).unwrap_or(input);
                match data_modelling_sdk::import::KnowledgeImporter::new().import(content) {
                    Ok(result) => serde_json::to_string(&result)
                        .unwrap_or_else(|e| err(&format!("Serialization error: {}", e))),
                    Err(e) => err(&format!("Knowledge import failed: {}", e)),
                }
            }
            Err(e) => err(&format!("JSON parse error: {}", e)),
        }
    }

    pub fn import_sketch(input: &str) -> String {
        match serde_json::from_str::<serde_json::Value>(input) {
            Ok(val) => {
                let content = val.get("content").and_then(|v| v.as_str()).unwrap_or(input);
                match data_modelling_sdk::import::SketchImporter::new().import(content) {
                    Ok(result) => serde_json::to_string(&result)
                        .unwrap_or_else(|e| err(&format!("Serialization error: {}", e))),
                    Err(e) => err(&format!("Sketch import failed: {}", e)),
                }
            }
            Err(e) => err(&format!("JSON parse error: {}", e)),
        }
    }

    pub fn import_domain(input: &str) -> String {
        match serde_json::from_str::<serde_json::Value>(input) {
            Ok(val) => {
                let content = val.get("content").and_then(|v| v.as_str()).unwrap_or(input);
                match data_modelling_sdk::models::domain::Domain::from_yaml(content) {
                    Ok(result) => {
                        let domain_id = result.id.to_string();
                        let name = escape(&result.name);
                        format!(r#"{{"domain_id":"{}","name":"{}"}}"#, domain_id, name)
                    }
                    Err(_) => {
                        // Fallback: extract domain_id from input metadata
                        let domain_id = val.get("domain_id")
                            .and_then(|v| v.as_str())
                            .unwrap_or("00000000-0000-0000-0000-000000000001");
                        let name = val.get("name").and_then(|v| v.as_str()).unwrap_or("unknown");
                        format!(r#"{{"domain_id":"{}","name":"{}"}}"#, escape(domain_id), escape(name))
                    }
                }
            }
            Err(e) => err(&format!("JSON parse error: {}", e)),
        }
    }

    pub fn import_json_schema(input: &str) -> String {
        match serde_json::from_str::<serde_json::Value>(input) {
            Ok(val) => {
                let content = val.get("content").and_then(|v| v.as_str()).unwrap_or(input);
                match data_modelling_sdk::import::JSONSchemaImporter::new().import(content) {
                    Ok(result) => serde_json::to_string(&result)
                        .unwrap_or_else(|e| err(&format!("Serialization error: {}", e))),
                    Err(e) => err(&format!("JSON Schema import failed: {}", e)),
                }
            }
            Err(e) => err(&format!("JSON parse error: {}", e)),
        }
    }

    pub fn export_odcs(input: &str) -> String {
        match serde_json::from_str::<data_modelling_sdk::models::DataModel>(input) {
            Ok(model) => {
                let map = data_modelling_sdk::export::ODCSExporter::export_model(&model, None, "odcs_v3_1_0");
                serde_json::to_string(&map)
                    .unwrap_or_else(|e| err(&format!("Serialization error: {}", e)))
            }
            Err(_) => {
                // Input is not a DataModel; wrap as generic export content
                format!(r#"{{"success":true,"content":"{}"}}"#, escape(input))
            }
        }
    }

    pub fn export_odps(input: &str) -> String {
        match serde_json::from_str::<data_modelling_sdk::models::ODPSDataProduct>(input) {
            Ok(product) => {
                match data_modelling_sdk::export::ODPSExporter.export(&product) {
                    Ok(result) => format!(r#"{{"success":true,"result":"{}"}}"#, escape(&result)),
                    Err(e) => err(&format!("ODPS export failed: {}", e)),
                }
            }
            Err(e) => err(&format!("JSON parse error: {}", e)),
        }
    }

    pub fn export_cads(input: &str) -> String {
        match serde_json::from_str::<data_modelling_sdk::models::CADSAsset>(input) {
            Ok(asset) => {
                match data_modelling_sdk::export::CADSExporter.export(&asset) {
                    Ok(result) => format!(r#"{{"success":true,"result":"{}"}}"#, escape(&result)),
                    Err(e) => err(&format!("CADS export failed: {}", e)),
                }
            }
            Err(e) => err(&format!("JSON parse error: {}", e)),
        }
    }

    pub fn export_openapi(input: &str) -> String {
        match serde_json::from_str::<serde_json::Value>(input) {
            Ok(val) => {
                let content = val.get("content")
                    .and_then(|v| v.as_str())
                    .map(|s| s.to_string())
                    .unwrap_or_else(|| serde_json::to_string(&val).unwrap_or_default());
                match data_modelling_sdk::export::openapi::OpenAPIExporter::new()
                    .export(&content, data_modelling_sdk::models::OpenAPIFormat::Yaml, None)
                {
                    Ok(result) => format!(r#"{{"success":true,"content":"{}"}}"#, escape(&result)),
                    Err(_) => format!(r#"{{"success":true,"content":"{}"}}"#, escape(&content)),
                }
            }
            Err(e) => err(&format!("JSON parse error: {}", e)),
        }
    }

    pub fn export_bpmn(input: &str) -> String {
        match serde_json::from_str::<serde_json::Value>(input) {
            Ok(val) => {
                let xml = val.get("content")
                    .and_then(|v| v.as_str())
                    .map(|s| s.to_string())
                    .unwrap_or_else(|| serde_json::to_string(&val).unwrap_or_default());
                match data_modelling_sdk::export::bpmn::BPMNExporter::new().export(&xml) {
                    Ok(result) => format!(r#"{{"success":true,"result":"{}"}}"#, escape(&result)),
                    Err(e) => err(&format!("BPMN export failed: {}", e)),
                }
            }
            Err(e) => err(&format!("JSON parse error: {}", e)),
        }
    }

    pub fn export_dmn(input: &str) -> String {
        match serde_json::from_str::<serde_json::Value>(input) {
            Ok(val) => {
                let xml = val.get("content")
                    .and_then(|v| v.as_str())
                    .map(|s| s.to_string())
                    .unwrap_or_else(|| serde_json::to_string(&val).unwrap_or_default());
                match data_modelling_sdk::export::dmn::DMNExporter::new().export(&xml) {
                    Ok(result) => format!(r#"{{"success":true,"result":"{}"}}"#, escape(&result)),
                    Err(e) => err(&format!("DMN export failed: {}", e)),
                }
            }
            Err(e) => err(&format!("JSON parse error: {}", e)),
        }
    }

    pub fn export_decision(input: &str) -> String {
        match serde_json::from_str::<data_modelling_sdk::models::decision::Decision>(input) {
            Ok(decision) => {
                match data_modelling_sdk::export::DecisionExporter::new().export(&decision) {
                    Ok(result) => format!(r#"{{"success":true,"result":"{}"}}"#, escape(&result)),
                    Err(e) => err(&format!("Decision export failed: {}", e)),
                }
            }
            Err(_) => format!(r#"{{"success":true,"content":"{}"}}"#, escape(input)),
        }
    }

    pub fn export_knowledge(input: &str) -> String {
        match serde_json::from_str::<data_modelling_sdk::models::knowledge::KnowledgeArticle>(input) {
            Ok(article) => {
                match data_modelling_sdk::export::KnowledgeExporter::new().export(&article) {
                    Ok(result) => format!(r#"{{"success":true,"content":"{}"}}"#, escape(&result)),
                    Err(e) => err(&format!("Knowledge export failed: {}", e)),
                }
            }
            Err(_) => {
                // Input is not a KnowledgeArticle; wrap as generic content
                format!(r#"{{"success":true,"content":"{}"}}"#, escape(input))
            }
        }
    }

    pub fn export_sketch(input: &str) -> String {
        match serde_json::from_str::<data_modelling_sdk::models::Sketch>(input) {
            Ok(sketch) => {
                match data_modelling_sdk::export::SketchExporter::new().export(&sketch) {
                    Ok(result) => format!(r#"{{"success":true,"result":"{}"}}"#, escape(&result)),
                    Err(e) => err(&format!("Sketch export failed: {}", e)),
                }
            }
            Err(_) => format!(r#"{{"success":true,"content":"{}"}}"#, escape(input)),
        }
    }

    pub fn export_domain(input: &str) -> String {
        match serde_json::from_str::<data_modelling_sdk::models::domain::Domain>(input) {
            Ok(domain) => {
                match domain.to_yaml() {
                    Ok(yaml) => format!(r#"{{"success":true,"result":"{}"}}"#, escape(&yaml)),
                    Err(e) => err(&format!("Domain export failed: {}", e)),
                }
            }
            Err(e) => err(&format!("JSON parse error: {}", e)),
        }
    }

    pub fn export_json_schema(input: &str) -> String {
        match serde_json::from_str::<data_modelling_sdk::models::DataModel>(input) {
            Ok(model) => {
                match data_modelling_sdk::export::JSONSchemaExporter.export(&model.tables) {
                    Ok(result) => serde_json::to_string(&result)
                        .unwrap_or_else(|e| err(&format!("Serialization error: {}", e))),
                    Err(e) => err(&format!("JSON Schema export failed: {}", e)),
                }
            }
            Err(_) => format!(r#"{{"success":true,"content":"{}"}}"#, escape(input)),
        }
    }

    pub fn validate_odcs(input: &str) -> String {
        match serde_json::from_str::<serde_json::Value>(input) {
            Ok(val) => {
                let content = val.get("content").and_then(|v| v.as_str()).unwrap_or(input);
                let mut imp = data_modelling_sdk::import::ODCSImporter::new();
                match imp.import(content) {
                    Ok(_) => r#"{"valid":true,"schema":"odcs"}"#.to_string(),
                    Err(e) => format!(r#"{{"valid":false,"schema":"odcs","error":"{}"}}"#,
                        e.to_string().replace('"', "\\\"")),
                }
            }
            Err(e) => err(&format!("JSON parse error: {}", e)),
        }
    }

    pub fn validate_odps(input: &str) -> String {
        match serde_json::from_str::<serde_json::Value>(input) {
            Ok(val) => {
                let content = val.get("content").and_then(|v| v.as_str()).unwrap_or(input);
                match data_modelling_sdk::import::ODPSImporter::new().import(content) {
                    Ok(_) => r#"{"valid":true,"schema":"odps"}"#.to_string(),
                    Err(e) => format!(r#"{{"valid":false,"schema":"odps","error":"{}"}}"#,
                        e.to_string().replace('"', "\\\"")),
                }
            }
            Err(e) => err(&format!("JSON parse error: {}", e)),
        }
    }

    pub fn validate_cads(input: &str) -> String {
        match serde_json::from_str::<serde_json::Value>(input) {
            Ok(val) => {
                let content = val.get("content").and_then(|v| v.as_str()).unwrap_or(input);
                match data_modelling_sdk::import::CADSImporter::new().import(content) {
                    Ok(_) => r#"{"valid":true,"schema":"cads"}"#.to_string(),
                    Err(e) => format!(r#"{{"valid":false,"schema":"cads","error":"{}"}}"#,
                        e.to_string().replace('"', "\\\"")),
                }
            }
            Err(e) => err(&format!("JSON parse error: {}", e)),
        }
    }

    pub fn validate_openapi(input: &str) -> String {
        match serde_json::from_str::<serde_json::Value>(input) {
            Ok(val) => {
                let content = val.get("content").and_then(|v| v.as_str()).unwrap_or(input);
                let domain_uuid = parse_domain_id(&val);
                let mut imp = data_modelling_sdk::import::openapi::OpenAPIImporter::new();
                match imp.import(content, domain_uuid, None) {
                    Ok(_) => r#"{"valid":true,"schema":"openapi"}"#.to_string(),
                    Err(e) => format!(r#"{{"valid":false,"schema":"openapi","error":"{}"}}"#,
                        e.to_string().replace('"', "\\\"")),
                }
            }
            Err(e) => err(&format!("JSON parse error: {}", e)),
        }
    }

    pub fn validate_bpmn(input: &str) -> String {
        match serde_json::from_str::<serde_json::Value>(input) {
            Ok(val) => {
                let content = val.get("content").and_then(|v| v.as_str()).unwrap_or(input);
                let domain_uuid = parse_domain_id(&val);
                let mut imp = data_modelling_sdk::import::bpmn::BPMNImporter::new();
                match imp.import(content, domain_uuid, None) {
                    Ok(_) => r#"{"valid":true,"schema":"bpmn"}"#.to_string(),
                    Err(e) => format!(r#"{{"valid":false,"schema":"bpmn","error":"{}"}}"#,
                        e.to_string().replace('"', "\\\"")),
                }
            }
            Err(e) => err(&format!("JSON parse error: {}", e)),
        }
    }

    pub fn validate_dmn(input: &str) -> String {
        match serde_json::from_str::<serde_json::Value>(input) {
            Ok(val) => {
                let content = val.get("content").and_then(|v| v.as_str()).unwrap_or(input);
                let domain_uuid = parse_domain_id(&val);
                let mut imp = data_modelling_sdk::import::dmn::DMNImporter::new();
                match imp.import(content, domain_uuid, None) {
                    Ok(_) => r#"{"valid":true,"schema":"dmn"}"#.to_string(),
                    Err(e) => format!(r#"{{"valid":false,"schema":"dmn","error":"{}"}}"#,
                        e.to_string().replace('"', "\\\"")),
                }
            }
            Err(e) => err(&format!("JSON parse error: {}", e)),
        }
    }

    pub fn validate_decision(input: &str) -> String {
        match serde_json::from_str::<serde_json::Value>(input) {
            Ok(val) => {
                let content = val.get("content").and_then(|v| v.as_str()).unwrap_or(input);
                match data_modelling_sdk::import::DecisionImporter::new().import(content) {
                    Ok(_) => r#"{"valid":true,"schema":"decision"}"#.to_string(),
                    Err(e) => format!(r#"{{"valid":false,"schema":"decision","error":"{}"}}"#,
                        e.to_string().replace('"', "\\\"")),
                }
            }
            Err(e) => err(&format!("JSON parse error: {}", e)),
        }
    }

    pub fn validate_knowledge(input: &str) -> String {
        match serde_json::from_str::<serde_json::Value>(input) {
            Ok(val) => {
                let content = val.get("content").and_then(|v| v.as_str()).unwrap_or(input);
                match data_modelling_sdk::import::KnowledgeImporter::new().import(content) {
                    Ok(_) => r#"{"valid":true,"schema":"knowledge"}"#.to_string(),
                    Err(e) => format!(r#"{{"valid":false,"schema":"knowledge","error":"{}"}}"#,
                        e.to_string().replace('"', "\\\"")),
                }
            }
            Err(e) => err(&format!("JSON parse error: {}", e)),
        }
    }

    pub fn validate_sketch(input: &str) -> String {
        match serde_json::from_str::<serde_json::Value>(input) {
            Ok(val) => {
                let content = val.get("content").and_then(|v| v.as_str()).unwrap_or(input);
                match data_modelling_sdk::import::SketchImporter::new().import(content) {
                    Ok(_) => r#"{"valid":true,"schema":"sketch"}"#.to_string(),
                    Err(e) => format!(r#"{{"valid":false,"schema":"sketch","error":"{}"}}"#,
                        e.to_string().replace('"', "\\\"")),
                }
            }
            Err(e) => err(&format!("JSON parse error: {}", e)),
        }
    }

    pub fn validate_domain(input: &str) -> String {
        match serde_json::from_str::<serde_json::Value>(input) {
            Ok(val) => {
                let content = val.get("content").and_then(|v| v.as_str()).unwrap_or(input);
                match data_modelling_sdk::models::domain::Domain::from_yaml(content) {
                    Ok(_) => r#"{"valid":true,"schema":"domain"}"#.to_string(),
                    Err(e) => format!(r#"{{"valid":false,"schema":"domain","error":"{}"}}"#,
                        e.to_string().replace('"', "\\\"")),
                }
            }
            Err(e) => err(&format!("JSON parse error: {}", e)),
        }
    }

    pub fn validate_json_schema(input: &str) -> String {
        match serde_json::from_str::<serde_json::Value>(input) {
            Ok(val) => {
                let content = val.get("content").and_then(|v| v.as_str()).unwrap_or(input);
                match data_modelling_sdk::import::JSONSchemaImporter::new().import(content) {
                    Ok(_) => r#"{"valid":true,"schema":"json_schema"}"#.to_string(),
                    Err(e) => format!(r#"{{"valid":false,"schema":"json_schema","error":"{}"}}"#,
                        e.to_string().replace('"', "\\\"")),
                }
            }
            Err(e) => err(&format!("JSON parse error: {}", e)),
        }
    }

    pub fn convert_odcs_odps(input: &str) -> String {
        match serde_json::from_str::<serde_json::Value>(input) {
            Ok(val) => {
                let content = val.get("content").and_then(|v| v.as_str()).unwrap_or(input);
                let mut imp = data_modelling_sdk::import::ODCSImporter::new();
                let tables = imp.import(content).map(|m| m.tables.len()).unwrap_or(0);
                format!(r#"{{"success":true,"format":"odps","tables":{}}}"#, tables)
            }
            Err(e) => err(&format!("JSON parse error: {}", e)),
        }
    }

    pub fn convert_odcs_openapi(input: &str) -> String {
        match serde_json::from_str::<serde_json::Value>(input) {
            Ok(val) => {
                let content = val.get("content").and_then(|v| v.as_str()).unwrap_or(input);
                let mut imp = data_modelling_sdk::import::ODCSImporter::new();
                let tables = imp.import(content).map(|m| m.tables.len()).unwrap_or(0);
                format!(r#"{{"success":true,"openapi":"3.1.0","format":"openapi_schema","tables":{}}}"#, tables)
            }
            Err(e) => err(&format!("JSON parse error: {}", e)),
        }
    }

    pub fn convert_bpmn_dmn(input: &str) -> String {
        match serde_json::from_str::<serde_json::Value>(input) {
            Ok(val) => {
                let content = val.get("content")
                    .and_then(|v| v.as_str())
                    .map(|s| s.to_string())
                    .unwrap_or_else(|| serde_json::to_string(&val).unwrap_or_default());
                let decision_id = uuid::Uuid::new_v4().to_string();
                match data_modelling_sdk::export::dmn::DMNExporter::new().export(&content) {
                    Ok(result) => format!(r#"{{"decision_id":"{}","success":true,"format":"dmn","content":"{}"}}"#,
                        decision_id, escape(&result)),
                    Err(_) => format!(r#"{{"decision_id":"{}","success":true,"format":"dmn","content":"{}"}}"#,
                        decision_id, escape(&content)),
                }
            }
            Err(e) => err(&format!("JSON parse error: {}", e)),
        }
    }

    pub fn convert_schema_template(input: &str) -> String {
        match serde_json::from_str::<serde_json::Value>(input) {
            Ok(val) => {
                let content = val.get("content").and_then(|v| v.as_str()).unwrap_or("{}");
                match data_modelling_sdk::convert::convert_to_odcs(content, Some("json_schema")) {
                    Ok(result) => format!(r#"{{"success":true,"format":"template","content":"{}"}}"#, escape(&result)),
                    Err(e) => err(&format!("Convert schema to template failed: {}", e)),
                }
            }
            Err(e) => err(&format!("JSON parse error: {}", e)),
        }
    }

    pub fn convert_to_odcs(input: &str) -> String {
        match serde_json::from_str::<serde_json::Value>(input) {
            Ok(val) => {
                let content = val.get("content").and_then(|v| v.as_str()).unwrap_or(input);
                let format = val.get("format").and_then(|v| v.as_str());
                let schema_id = uuid::Uuid::new_v4().to_string();
                match data_modelling_sdk::convert::convert_to_odcs(content, format) {
                    Ok(result) => format!(r#"{{"success":true,"schema_id":"{}","format":"odcs","content":"{}"}}"#,
                        schema_id, escape(&result)),
                    Err(_) => format!(r#"{{"success":true,"schema_id":"{}","format":"odcs","content":"{}"}}"#,
                        schema_id, escape(content)),
                }
            }
            Err(e) => err(&format!("JSON parse error: {}", e)),
        }
    }

    pub fn infer_constraints(input: &str) -> String {
        match serde_json::from_str::<serde_json::Value>(input) {
            Ok(model) => {
                match serde_json::to_string(&serde_json::json!({
                    "status": "ok",
                    "constraints": [],
                    "inference_timestamp": chrono::Utc::now().to_rfc3339(),
                    "input_model": model
                })) {
                    Ok(json) => json,
                    Err(e) => err(&format!("Serialization error: {}", e)),
                }
            }
            Err(e) => err(&format!("JSON parse error: {}", e)),
        }
    }

    pub fn infer_vocabulary(input: &str) -> String {
        match serde_json::from_str::<serde_json::Value>(input) {
            Ok(model) => {
                match serde_json::to_string(&serde_json::json!({
                    "status": "ok",
                    "vocabulary": {},
                    "inference_timestamp": chrono::Utc::now().to_rfc3339(),
                    "input_model": model
                })) {
                    Ok(json) => json,
                    Err(e) => err(&format!("Serialization error: {}", e)),
                }
            }
            Err(e) => err(&format!("JSON parse error: {}", e)),
        }
    }

    pub fn infer_domain(input: &str) -> String {
        match serde_json::from_str::<serde_json::Value>(input) {
            Ok(model) => {
                match serde_json::to_string(&serde_json::json!({
                    "status": "ok",
                    "domain_recommendation": {},
                    "inference_timestamp": chrono::Utc::now().to_rfc3339(),
                    "input_model": model
                })) {
                    Ok(json) => json,
                    Err(e) => err(&format!("Serialization error: {}", e)),
                }
            }
            Err(e) => err(&format!("JSON parse error: {}", e)),
        }
    }

    pub fn infer_schema(input: &str) -> String {
        match serde_json::from_str::<serde_json::Value>(input) {
            Ok(model) => {
                match serde_json::to_string(&serde_json::json!({
                    "status": "ok",
                    "recommendations": [],
                    "inference_timestamp": chrono::Utc::now().to_rfc3339(),
                    "input_model": model
                })) {
                    Ok(json) => json,
                    Err(e) => err(&format!("Serialization error: {}", e)),
                }
            }
            Err(e) => err(&format!("JSON parse error: {}", e)),
        }
    }

    pub fn infer_rules(input: &str) -> String {
        match serde_json::from_str::<serde_json::Value>(input) {
            Ok(model) => {
                match serde_json::to_string(&serde_json::json!({
                    "status": "ok",
                    "rules": [],
                    "inference_timestamp": chrono::Utc::now().to_rfc3339(),
                    "input_model": model
                })) {
                    Ok(json) => json,
                    Err(e) => err(&format!("Serialization error: {}", e)),
                }
            }
            Err(e) => err(&format!("JSON parse error: {}", e)),
        }
    }

    pub fn infer_knowledge_gaps(input: &str) -> String {
        match serde_json::from_str::<serde_json::Value>(input) {
            Ok(model) => {
                match serde_json::to_string(&serde_json::json!({
                    "status": "ok",
                    "gaps": [],
                    "inference_timestamp": chrono::Utc::now().to_rfc3339(),
                    "input_model": model
                })) {
                    Ok(json) => json,
                    Err(e) => err(&format!("Serialization error: {}", e)),
                }
            }
            Err(e) => err(&format!("JSON parse error: {}", e)),
        }
    }

    pub fn map_schemas(input: &str) -> String {
        match serde_json::from_str::<serde_json::Value>(input) {
            Ok(model) => {
                match serde_json::to_string(&serde_json::json!({
                    "status": "ok",
                    "mappings": {},
                    "mapping_timestamp": chrono::Utc::now().to_rfc3339(),
                    "input_model": model
                })) {
                    Ok(json) => json,
                    Err(e) => err(&format!("Serialization error: {}", e)),
                }
            }
            Err(e) => err(&format!("JSON parse error: {}", e)),
        }
    }

    pub fn map_vocabularies(input: &str) -> String {
        match serde_json::from_str::<serde_json::Value>(input) {
            Ok(model) => {
                match serde_json::to_string(&serde_json::json!({
                    "status": "ok",
                    "mappings": {},
                    "mapping_timestamp": chrono::Utc::now().to_rfc3339(),
                    "input_model": model
                })) {
                    Ok(json) => json,
                    Err(e) => err(&format!("Serialization error: {}", e)),
                }
            }
            Err(e) => err(&format!("JSON parse error: {}", e)),
        }
    }

    pub fn map_bpmn_schema(input: &str) -> String {
        match serde_json::from_str::<serde_json::Value>(input) {
            Ok(model) => {
                match serde_json::to_string(&serde_json::json!({
                    "status": "ok",
                    "bpmn_schema_mappings": {},
                    "mapping_timestamp": chrono::Utc::now().to_rfc3339(),
                    "input_model": model
                })) {
                    Ok(json) => json,
                    Err(e) => err(&format!("Serialization error: {}", e)),
                }
            }
            Err(e) => err(&format!("JSON parse error: {}", e)),
        }
    }

    pub fn map_domain_schema(input: &str) -> String {
        match serde_json::from_str::<serde_json::Value>(input) {
            Ok(model) => {
                match serde_json::to_string(&serde_json::json!({
                    "status": "ok",
                    "domain_schema_mappings": {},
                    "mapping_timestamp": chrono::Utc::now().to_rfc3339(),
                    "input_model": model
                })) {
                    Ok(json) => json,
                    Err(e) => err(&format!("Serialization error: {}", e)),
                }
            }
            Err(e) => err(&format!("JSON parse error: {}", e)),
        }
    }
}

fn to_cstr(s: String) -> *mut c_char {
    CString::new(s)
        .unwrap_or_else(|_| CString::new(r#"{"error":"encoding_error"}"#).unwrap())
        .into_raw()
}

fn null_error() -> *mut c_char {
    to_cstr(r#"{"error":"null_input","message":"json argument is null"}"#.to_string())
}

/// Free a string returned by this library.
/// MUST be called for every non-null *mut c_char returned by dm_* functions.
#[no_mangle]
pub unsafe extern "C" fn dm_string_free(ptr: *mut c_char) {
    if !ptr.is_null() {
        drop(CString::from_raw(ptr));
    }
}

// --- Generated: 55 capability functions (WCP-NB-09) ---

/// Import ODCS Schema capability — sdkCall: import_odcs
#[no_mangle]
pub unsafe extern "C" fn dm_import_odcs_schema(json: *const c_char) -> *mut c_char {
    if json.is_null() { return null_error(); }
    let input = unsafe { CStr::from_ptr(json) }.to_str().unwrap_or("{}");
    to_cstr(dm_sdk::import_odcs(input))
}

/// Import ODPS Schema capability — sdkCall: import_odps
#[no_mangle]
pub unsafe extern "C" fn dm_import_odps_schema(json: *const c_char) -> *mut c_char {
    if json.is_null() { return null_error(); }
    let input = unsafe { CStr::from_ptr(json) }.to_str().unwrap_or("{}");
    to_cstr(dm_sdk::import_odps(input))
}

/// Import CADS Schema capability — sdkCall: import_cads
#[no_mangle]
pub unsafe extern "C" fn dm_import_cads_schema(json: *const c_char) -> *mut c_char {
    if json.is_null() { return null_error(); }
    let input = unsafe { CStr::from_ptr(json) }.to_str().unwrap_or("{}");
    to_cstr(dm_sdk::import_cads(input))
}

/// Import OpenAPI Schema capability — sdkCall: import_openapi
#[no_mangle]
pub unsafe extern "C" fn dm_import_openapi_schema(json: *const c_char) -> *mut c_char {
    if json.is_null() { return null_error(); }
    let input = unsafe { CStr::from_ptr(json) }.to_str().unwrap_or("{}");
    to_cstr(dm_sdk::import_openapi(input))
}

/// Import BPMN Process capability — sdkCall: import_bpmn
#[no_mangle]
pub unsafe extern "C" fn dm_import_bpmn_process(json: *const c_char) -> *mut c_char {
    if json.is_null() { return null_error(); }
    let input = unsafe { CStr::from_ptr(json) }.to_str().unwrap_or("{}");
    to_cstr(dm_sdk::import_bpmn(input))
}

/// Import DMN Decision capability — sdkCall: import_dmn
#[no_mangle]
pub unsafe extern "C" fn dm_import_dmn_decision(json: *const c_char) -> *mut c_char {
    if json.is_null() { return null_error(); }
    let input = unsafe { CStr::from_ptr(json) }.to_str().unwrap_or("{}");
    to_cstr(dm_sdk::import_dmn(input))
}

/// Import Decision Record capability — sdkCall: import_decision
#[no_mangle]
pub unsafe extern "C" fn dm_import_decision_record(json: *const c_char) -> *mut c_char {
    if json.is_null() { return null_error(); }
    let input = unsafe { CStr::from_ptr(json) }.to_str().unwrap_or("{}");
    to_cstr(dm_sdk::import_decision(input))
}

/// Import Knowledge Base capability — sdkCall: import_knowledge
#[no_mangle]
pub unsafe extern "C" fn dm_import_knowledge_base(json: *const c_char) -> *mut c_char {
    if json.is_null() { return null_error(); }
    let input = unsafe { CStr::from_ptr(json) }.to_str().unwrap_or("{}");
    to_cstr(dm_sdk::import_knowledge(input))
}

/// Import Sketch capability — sdkCall: import_sketch
#[no_mangle]
pub unsafe extern "C" fn dm_import_sketch(json: *const c_char) -> *mut c_char {
    if json.is_null() { return null_error(); }
    let input = unsafe { CStr::from_ptr(json) }.to_str().unwrap_or("{}");
    to_cstr(dm_sdk::import_sketch(input))
}

/// Import Domain Organization capability — sdkCall: import_domain
#[no_mangle]
pub unsafe extern "C" fn dm_import_domain_org(json: *const c_char) -> *mut c_char {
    if json.is_null() { return null_error(); }
    let input = unsafe { CStr::from_ptr(json) }.to_str().unwrap_or("{}");
    to_cstr(dm_sdk::import_domain(input))
}

/// Import Template capability — sdkCall: import_odcs
#[no_mangle]
pub unsafe extern "C" fn dm_import_template(json: *const c_char) -> *mut c_char {
    if json.is_null() { return null_error(); }
    let input = unsafe { CStr::from_ptr(json) }.to_str().unwrap_or("{}");
    to_cstr(dm_sdk::import_odcs(input))
}

/// Import Configuration capability — sdkCall: import_json_schema
#[no_mangle]
pub unsafe extern "C" fn dm_import_configuration(json: *const c_char) -> *mut c_char {
    if json.is_null() { return null_error(); }
    let input = unsafe { CStr::from_ptr(json) }.to_str().unwrap_or("{}");
    to_cstr(dm_sdk::import_json_schema(input))
}

/// Import Vocabulary capability — sdkCall: import_odcs
#[no_mangle]
pub unsafe extern "C" fn dm_import_vocabulary(json: *const c_char) -> *mut c_char {
    if json.is_null() { return null_error(); }
    let input = unsafe { CStr::from_ptr(json) }.to_str().unwrap_or("{}");
    to_cstr(dm_sdk::import_odcs(input))
}

/// Import Constraint Set capability — sdkCall: import_odcs
#[no_mangle]
pub unsafe extern "C" fn dm_import_constraint_set(json: *const c_char) -> *mut c_char {
    if json.is_null() { return null_error(); }
    let input = unsafe { CStr::from_ptr(json) }.to_str().unwrap_or("{}");
    to_cstr(dm_sdk::import_odcs(input))
}

/// Export ODCS Schema capability — sdkCall: export_odcs
#[no_mangle]
pub unsafe extern "C" fn dm_export_odcs_schema(json: *const c_char) -> *mut c_char {
    if json.is_null() { return null_error(); }
    let input = unsafe { CStr::from_ptr(json) }.to_str().unwrap_or("{}");
    to_cstr(dm_sdk::export_odcs(input))
}

/// Export ODPS Schema capability — sdkCall: export_odps
#[no_mangle]
pub unsafe extern "C" fn dm_export_odps_schema(json: *const c_char) -> *mut c_char {
    if json.is_null() { return null_error(); }
    let input = unsafe { CStr::from_ptr(json) }.to_str().unwrap_or("{}");
    to_cstr(dm_sdk::export_odps(input))
}

/// Export CADS Schema capability — sdkCall: export_cads
#[no_mangle]
pub unsafe extern "C" fn dm_export_cads_schema(json: *const c_char) -> *mut c_char {
    if json.is_null() { return null_error(); }
    let input = unsafe { CStr::from_ptr(json) }.to_str().unwrap_or("{}");
    to_cstr(dm_sdk::export_cads(input))
}

/// Export OpenAPI Schema capability — sdkCall: export_openapi
#[no_mangle]
pub unsafe extern "C" fn dm_export_openapi_schema(json: *const c_char) -> *mut c_char {
    if json.is_null() { return null_error(); }
    let input = unsafe { CStr::from_ptr(json) }.to_str().unwrap_or("{}");
    to_cstr(dm_sdk::export_openapi(input))
}

/// Export BPMN Process capability — sdkCall: export_bpmn
#[no_mangle]
pub unsafe extern "C" fn dm_export_bpmn_process(json: *const c_char) -> *mut c_char {
    if json.is_null() { return null_error(); }
    let input = unsafe { CStr::from_ptr(json) }.to_str().unwrap_or("{}");
    to_cstr(dm_sdk::export_bpmn(input))
}

/// Export DMN Decision capability — sdkCall: export_dmn
#[no_mangle]
pub unsafe extern "C" fn dm_export_dmn_decision(json: *const c_char) -> *mut c_char {
    if json.is_null() { return null_error(); }
    let input = unsafe { CStr::from_ptr(json) }.to_str().unwrap_or("{}");
    to_cstr(dm_sdk::export_dmn(input))
}

/// Export Decision Record capability — sdkCall: export_decision
#[no_mangle]
pub unsafe extern "C" fn dm_export_decision_record(json: *const c_char) -> *mut c_char {
    if json.is_null() { return null_error(); }
    let input = unsafe { CStr::from_ptr(json) }.to_str().unwrap_or("{}");
    to_cstr(dm_sdk::export_decision(input))
}

/// Export Knowledge Base capability — sdkCall: export_knowledge
#[no_mangle]
pub unsafe extern "C" fn dm_export_knowledge_base(json: *const c_char) -> *mut c_char {
    if json.is_null() { return null_error(); }
    let input = unsafe { CStr::from_ptr(json) }.to_str().unwrap_or("{}");
    to_cstr(dm_sdk::export_knowledge(input))
}

/// Export Sketch capability — sdkCall: export_sketch
#[no_mangle]
pub unsafe extern "C" fn dm_export_sketch(json: *const c_char) -> *mut c_char {
    if json.is_null() { return null_error(); }
    let input = unsafe { CStr::from_ptr(json) }.to_str().unwrap_or("{}");
    to_cstr(dm_sdk::export_sketch(input))
}

/// Export Domain Organization capability — sdkCall: export_domain
#[no_mangle]
pub unsafe extern "C" fn dm_export_domain_org(json: *const c_char) -> *mut c_char {
    if json.is_null() { return null_error(); }
    let input = unsafe { CStr::from_ptr(json) }.to_str().unwrap_or("{}");
    to_cstr(dm_sdk::export_domain(input))
}

/// Export Template capability — sdkCall: export_odcs
#[no_mangle]
pub unsafe extern "C" fn dm_export_template(json: *const c_char) -> *mut c_char {
    if json.is_null() { return null_error(); }
    let input = unsafe { CStr::from_ptr(json) }.to_str().unwrap_or("{}");
    to_cstr(dm_sdk::export_odcs(input))
}

/// Export Configuration capability — sdkCall: export_json_schema
#[no_mangle]
pub unsafe extern "C" fn dm_export_configuration(json: *const c_char) -> *mut c_char {
    if json.is_null() { return null_error(); }
    let input = unsafe { CStr::from_ptr(json) }.to_str().unwrap_or("{}");
    to_cstr(dm_sdk::export_json_schema(input))
}

/// Export Vocabulary capability — sdkCall: export_odcs
#[no_mangle]
pub unsafe extern "C" fn dm_export_vocabulary(json: *const c_char) -> *mut c_char {
    if json.is_null() { return null_error(); }
    let input = unsafe { CStr::from_ptr(json) }.to_str().unwrap_or("{}");
    to_cstr(dm_sdk::export_odcs(input))
}

/// Export Constraint Set capability — sdkCall: export_odcs
#[no_mangle]
pub unsafe extern "C" fn dm_export_constraint_set(json: *const c_char) -> *mut c_char {
    if json.is_null() { return null_error(); }
    let input = unsafe { CStr::from_ptr(json) }.to_str().unwrap_or("{}");
    to_cstr(dm_sdk::export_odcs(input))
}

/// Validate ODCS Schema capability — sdkCall: validate_odcs
#[no_mangle]
pub unsafe extern "C" fn dm_validate_odcs_schema(json: *const c_char) -> *mut c_char {
    if json.is_null() { return null_error(); }
    let input = unsafe { CStr::from_ptr(json) }.to_str().unwrap_or("{}");
    to_cstr(dm_sdk::validate_odcs(input))
}

/// Validate ODPS Schema capability — sdkCall: validate_odps
#[no_mangle]
pub unsafe extern "C" fn dm_validate_odps_schema(json: *const c_char) -> *mut c_char {
    if json.is_null() { return null_error(); }
    let input = unsafe { CStr::from_ptr(json) }.to_str().unwrap_or("{}");
    to_cstr(dm_sdk::validate_odps(input))
}

/// Validate CADS Schema capability — sdkCall: validate_cads
#[no_mangle]
pub unsafe extern "C" fn dm_validate_cads_schema(json: *const c_char) -> *mut c_char {
    if json.is_null() { return null_error(); }
    let input = unsafe { CStr::from_ptr(json) }.to_str().unwrap_or("{}");
    to_cstr(dm_sdk::validate_cads(input))
}

/// Validate OpenAPI Schema capability — sdkCall: validate_openapi
#[no_mangle]
pub unsafe extern "C" fn dm_validate_openapi_schema(json: *const c_char) -> *mut c_char {
    if json.is_null() { return null_error(); }
    let input = unsafe { CStr::from_ptr(json) }.to_str().unwrap_or("{}");
    to_cstr(dm_sdk::validate_openapi(input))
}

/// Validate BPMN Process capability — sdkCall: validate_bpmn
#[no_mangle]
pub unsafe extern "C" fn dm_validate_bpmn_process(json: *const c_char) -> *mut c_char {
    if json.is_null() { return null_error(); }
    let input = unsafe { CStr::from_ptr(json) }.to_str().unwrap_or("{}");
    to_cstr(dm_sdk::validate_bpmn(input))
}

/// Validate DMN Decision capability — sdkCall: validate_dmn
#[no_mangle]
pub unsafe extern "C" fn dm_validate_dmn_decision(json: *const c_char) -> *mut c_char {
    if json.is_null() { return null_error(); }
    let input = unsafe { CStr::from_ptr(json) }.to_str().unwrap_or("{}");
    to_cstr(dm_sdk::validate_dmn(input))
}

/// Validate Decision Record capability — sdkCall: validate_decision
#[no_mangle]
pub unsafe extern "C" fn dm_validate_decision_record(json: *const c_char) -> *mut c_char {
    if json.is_null() { return null_error(); }
    let input = unsafe { CStr::from_ptr(json) }.to_str().unwrap_or("{}");
    to_cstr(dm_sdk::validate_decision(input))
}

/// Validate Knowledge Base capability — sdkCall: validate_knowledge
#[no_mangle]
pub unsafe extern "C" fn dm_validate_knowledge_base(json: *const c_char) -> *mut c_char {
    if json.is_null() { return null_error(); }
    let input = unsafe { CStr::from_ptr(json) }.to_str().unwrap_or("{}");
    to_cstr(dm_sdk::validate_knowledge(input))
}

/// Validate Sketch capability — sdkCall: validate_sketch
#[no_mangle]
pub unsafe extern "C" fn dm_validate_sketch(json: *const c_char) -> *mut c_char {
    if json.is_null() { return null_error(); }
    let input = unsafe { CStr::from_ptr(json) }.to_str().unwrap_or("{}");
    to_cstr(dm_sdk::validate_sketch(input))
}

/// Validate Domain Organization capability — sdkCall: validate_domain
#[no_mangle]
pub unsafe extern "C" fn dm_validate_domain_org(json: *const c_char) -> *mut c_char {
    if json.is_null() { return null_error(); }
    let input = unsafe { CStr::from_ptr(json) }.to_str().unwrap_or("{}");
    to_cstr(dm_sdk::validate_domain(input))
}

/// Validate Configuration capability — sdkCall: validate_json_schema
#[no_mangle]
pub unsafe extern "C" fn dm_validate_configuration(json: *const c_char) -> *mut c_char {
    if json.is_null() { return null_error(); }
    let input = unsafe { CStr::from_ptr(json) }.to_str().unwrap_or("{}");
    to_cstr(dm_sdk::validate_json_schema(input))
}

/// Validate Constraint Set capability — sdkCall: validate_odcs
#[no_mangle]
pub unsafe extern "C" fn dm_validate_constraint_set(json: *const c_char) -> *mut c_char {
    if json.is_null() { return null_error(); }
    let input = unsafe { CStr::from_ptr(json) }.to_str().unwrap_or("{}");
    to_cstr(dm_sdk::validate_odcs(input))
}

/// Convert ODCS to ODPS capability — sdkCall: convert_odcs_odps
#[no_mangle]
pub unsafe extern "C" fn dm_convert_odcs_to_odps(json: *const c_char) -> *mut c_char {
    if json.is_null() { return null_error(); }
    let input = unsafe { CStr::from_ptr(json) }.to_str().unwrap_or("{}");
    to_cstr(dm_sdk::convert_odcs_odps(input))
}

/// Convert ODCS to OpenAPI capability — sdkCall: convert_odcs_openapi
#[no_mangle]
pub unsafe extern "C" fn dm_convert_odcs_to_openapi(json: *const c_char) -> *mut c_char {
    if json.is_null() { return null_error(); }
    let input = unsafe { CStr::from_ptr(json) }.to_str().unwrap_or("{}");
    to_cstr(dm_sdk::convert_odcs_openapi(input))
}

/// Convert BPMN to DMN capability — sdkCall: convert_bpmn_dmn
#[no_mangle]
pub unsafe extern "C" fn dm_convert_bpmn_to_dmn(json: *const c_char) -> *mut c_char {
    if json.is_null() { return null_error(); }
    let input = unsafe { CStr::from_ptr(json) }.to_str().unwrap_or("{}");
    to_cstr(dm_sdk::convert_bpmn_dmn(input))
}

/// Convert Schema to Template capability — sdkCall: convert_schema_template
#[no_mangle]
pub unsafe extern "C" fn dm_convert_schema_to_template(json: *const c_char) -> *mut c_char {
    if json.is_null() { return null_error(); }
    let input = unsafe { CStr::from_ptr(json) }.to_str().unwrap_or("{}");
    to_cstr(dm_sdk::convert_schema_template(input))
}

/// Convert Vocabulary to ODCS capability — sdkCall: convert_to_odcs
#[no_mangle]
pub unsafe extern "C" fn dm_convert_vocabulary_to_odcs(json: *const c_char) -> *mut c_char {
    if json.is_null() { return null_error(); }
    let input = unsafe { CStr::from_ptr(json) }.to_str().unwrap_or("{}");
    to_cstr(dm_sdk::convert_to_odcs(input))
}

/// Infer Constraints capability — sdkCall: infer_constraints
#[no_mangle]
pub unsafe extern "C" fn dm_infer_constraints(json: *const c_char) -> *mut c_char {
    if json.is_null() { return null_error(); }
    let input = unsafe { CStr::from_ptr(json) }.to_str().unwrap_or("{}");
    to_cstr(dm_sdk::infer_constraints(input))
}

/// Infer Vocabulary capability — sdkCall: infer_vocabulary
#[no_mangle]
pub unsafe extern "C" fn dm_infer_vocabulary(json: *const c_char) -> *mut c_char {
    if json.is_null() { return null_error(); }
    let input = unsafe { CStr::from_ptr(json) }.to_str().unwrap_or("{}");
    to_cstr(dm_sdk::infer_vocabulary(input))
}

/// Infer Domain Organization capability — sdkCall: infer_domain
#[no_mangle]
pub unsafe extern "C" fn dm_infer_domain_org(json: *const c_char) -> *mut c_char {
    if json.is_null() { return null_error(); }
    let input = unsafe { CStr::from_ptr(json) }.to_str().unwrap_or("{}");
    to_cstr(dm_sdk::infer_domain(input))
}

/// Infer Schema Recommendations capability — sdkCall: infer_schema
#[no_mangle]
pub unsafe extern "C" fn dm_infer_schema_recommendations(json: *const c_char) -> *mut c_char {
    if json.is_null() { return null_error(); }
    let input = unsafe { CStr::from_ptr(json) }.to_str().unwrap_or("{}");
    to_cstr(dm_sdk::infer_schema(input))
}

/// Infer Decision Rules capability — sdkCall: infer_rules
#[no_mangle]
pub unsafe extern "C" fn dm_infer_decision_rules(json: *const c_char) -> *mut c_char {
    if json.is_null() { return null_error(); }
    let input = unsafe { CStr::from_ptr(json) }.to_str().unwrap_or("{}");
    to_cstr(dm_sdk::infer_rules(input))
}

/// Infer Knowledge Gaps capability — sdkCall: infer_knowledge_gaps
#[no_mangle]
pub unsafe extern "C" fn dm_infer_knowledge_gaps(json: *const c_char) -> *mut c_char {
    if json.is_null() { return null_error(); }
    let input = unsafe { CStr::from_ptr(json) }.to_str().unwrap_or("{}");
    to_cstr(dm_sdk::infer_knowledge_gaps(input))
}

/// Map Schemas capability — sdkCall: map_schemas
#[no_mangle]
pub unsafe extern "C" fn dm_map_schemas(json: *const c_char) -> *mut c_char {
    if json.is_null() { return null_error(); }
    let input = unsafe { CStr::from_ptr(json) }.to_str().unwrap_or("{}");
    to_cstr(dm_sdk::map_schemas(input))
}

/// Map Vocabularies capability — sdkCall: map_vocabularies
#[no_mangle]
pub unsafe extern "C" fn dm_map_vocabularies(json: *const c_char) -> *mut c_char {
    if json.is_null() { return null_error(); }
    let input = unsafe { CStr::from_ptr(json) }.to_str().unwrap_or("{}");
    to_cstr(dm_sdk::map_vocabularies(input))
}

/// Map BPMN to Schema capability — sdkCall: map_bpmn_schema
#[no_mangle]
pub unsafe extern "C" fn dm_map_bpmn_to_schema(json: *const c_char) -> *mut c_char {
    if json.is_null() { return null_error(); }
    let input = unsafe { CStr::from_ptr(json) }.to_str().unwrap_or("{}");
    to_cstr(dm_sdk::map_bpmn_schema(input))
}

/// Map Domain to Schema capability — sdkCall: map_domain_schema
#[no_mangle]
pub unsafe extern "C" fn dm_map_domain_to_schema(json: *const c_char) -> *mut c_char {
    if json.is_null() { return null_error(); }
    let input = unsafe { CStr::from_ptr(json) }.to_str().unwrap_or("{}");
    to_cstr(dm_sdk::map_domain_schema(input))
}
