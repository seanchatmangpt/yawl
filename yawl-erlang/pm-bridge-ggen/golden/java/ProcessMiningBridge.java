// Generated by gen-ttl. DO NOT EDIT.
package org.yawlfoundation.yawl.erlang.processmining;

import org.yawlfoundation.yawl.erlang.bridge.ErlangNode;
import org.yawlfoundation.yawl.erlang.error.ErlangRpcException;
import org.yawlfoundation.yawl.erlang.term.ErlAtom;
import org.yawlfoundation.yawl.erlang.term.ErlBinary;
import org.yawlfoundation.yawl.erlang.term.ErlFloat;
import org.yawlfoundation.yawl.erlang.term.ErlInteger;
import org.yawlfoundation.yawl.erlang.term.ErlTerm;
import java.nio.charset.StandardCharsets;
import java.util.List;

/**
* Java bridge to the process_mining Erlang gen_server.
* Each method performs an RPC call to process_mining_bridge.
*/
public class ProcessMiningBridge {

private static final String MODULE = "process_mining_bridge";
private final ErlangNode node;

public ProcessMiningBridge(ErlangNode node) {
this.node = node;
}

    /** Generate dotted chart data from an event log. */
public String getDottedChart(String xes, String options) throws ErlangRpcException {
ErlTerm result = node.rpc(MODULE, "get_dotted_chart", List.of(new ErlBinary(xes.getBytes(StandardCharsets.UTF_8)), new ErlBinary(options.getBytes(StandardCharsets.UTF_8))));
if (result instanceof ErlBinary eb) {
return new String(eb.data(), StandardCharsets.UTF_8);
}
return result.toString();
}

    /** Aggregate event timestamps into a fixed number of bins, grouped by activity. */
public String getEventTimestamps(String log, String options) throws ErlangRpcException {
ErlTerm result = node.rpc(MODULE, "get_event_timestamps", List.of(new ErlBinary(log.getBytes(StandardCharsets.UTF_8)), new ErlBinary(options.getBytes(StandardCharsets.UTF_8))));
if (result instanceof ErlBinary eb) {
return new String(eb.data(), StandardCharsets.UTF_8);
}
return result.toString();
}

    /** Extract the attribute change history for a specific object. */
public String getObjectAttributeChanges(String ocel, String objectId) throws ErlangRpcException {
ErlTerm result = node.rpc(MODULE, "get_object_attribute_changes", List.of(new ErlBinary(ocel.getBytes(StandardCharsets.UTF_8)), new ErlBinary(objectId.getBytes(StandardCharsets.UTF_8))));
if (result instanceof ErlBinary eb) {
return new String(eb.data(), StandardCharsets.UTF_8);
}
return result.toString();
}

    /** Returns the confidence conformance of an OC-DECLARE arc on the given OCEL */
public String ocDeclareConformance(String ocel, String arc) throws ErlangRpcException {
ErlTerm result = node.rpc(MODULE, "oc_declare_conformance", List.of(new ErlBinary(ocel.getBytes(StandardCharsets.UTF_8)), new ErlBinary(arc.getBytes(StandardCharsets.UTF_8))));
if (result instanceof ErlBinary eb) {
return new String(eb.data(), StandardCharsets.UTF_8);
}
return result.toString();
}

    /** Discover a [`PetriNet`] using the Alpha+++ Process Discovery algorithm */
public String discoverAlphaPpp(String logProj, String config) throws ErlangRpcException {
ErlTerm result = node.rpc(MODULE, "discover_alpha_ppp", List.of(new ErlBinary(logProj.getBytes(StandardCharsets.UTF_8)), new ErlBinary(config.getBytes(StandardCharsets.UTF_8))));
if (result instanceof ErlBinary eb) {
return new String(eb.data(), StandardCharsets.UTF_8);
}
return result.toString();
}

    /** Discover [`DirectlyFollowsGraph`] with default classifier */
public String discoverDfg(String eventLog) throws ErlangRpcException {
ErlTerm result = node.rpc(MODULE, "discover_dfg", List.of(new ErlBinary(eventLog.getBytes(StandardCharsets.UTF_8))));
if (result instanceof ErlBinary eb) {
return new String(eb.data(), StandardCharsets.UTF_8);
}
return result.toString();
}

    /** Construct an [`OCDirectlyFollowsGraph`] from an OCEL */
public String discoverDfgFromOcel(String ocel) throws ErlangRpcException {
ErlTerm result = node.rpc(MODULE, "discover_dfg_from_ocel", List.of(new ErlBinary(ocel.getBytes(StandardCharsets.UTF_8))));
if (result instanceof ErlBinary eb) {
return new String(eb.data(), StandardCharsets.UTF_8);
}
return result.toString();
}

    /** Discover behavioral OC-DECLARE constraints from the given OCEL */
public String discoverOcDeclare(String locel, String options) throws ErlangRpcException {
ErlTerm result = node.rpc(MODULE, "discover_oc_declare", List.of(new ErlBinary(locel.getBytes(StandardCharsets.UTF_8)), new ErlBinary(options.getBytes(StandardCharsets.UTF_8))));
if (result instanceof ErlBinary eb) {
return new String(eb.data(), StandardCharsets.UTF_8);
}
return result.toString();
}

    /** Add artificial init/exit events to an OCEL */
public String addInitExitEventsToOcel(String ocel) throws ErlangRpcException {
ErlTerm result = node.rpc(MODULE, "add_init_exit_events_to_ocel", List.of(new ErlBinary(ocel.getBytes(StandardCharsets.UTF_8))));
if (result instanceof ErlBinary eb) {
return new String(eb.data(), StandardCharsets.UTF_8);
}
return result.toString();
}

    /** Export the image of a [`DirectlyFollowsGraph`] as a PNG file */
public String exportDfgImagePng(String dfg, String path) throws ErlangRpcException {
ErlTerm result = node.rpc(MODULE, "export_dfg_image_png", List.of(new ErlBinary(dfg.getBytes(StandardCharsets.UTF_8)), new ErlBinary(path.getBytes(StandardCharsets.UTF_8))));
if (result instanceof ErlBinary eb) {
return new String(eb.data(), StandardCharsets.UTF_8);
}
return result.toString();
}

    /** Export the image of a [`DirectlyFollowsGraph`] as a SVG file */
public String exportDfgImageSvg(String dfg, String path) throws ErlangRpcException {
ErlTerm result = node.rpc(MODULE, "export_dfg_image_svg", List.of(new ErlBinary(dfg.getBytes(StandardCharsets.UTF_8)), new ErlBinary(path.getBytes(StandardCharsets.UTF_8))));
if (result instanceof ErlBinary eb) {
return new String(eb.data(), StandardCharsets.UTF_8);
}
return result.toString();
}

    /** Export Dfg Png */
public String exportDfgPng(String dfg, String path) throws ErlangRpcException {
ErlTerm result = node.rpc(MODULE, "export_dfg_png", List.of(new ErlBinary(dfg.getBytes(StandardCharsets.UTF_8)), new ErlBinary(path.getBytes(StandardCharsets.UTF_8))));
if (result instanceof ErlBinary eb) {
return new String(eb.data(), StandardCharsets.UTF_8);
}
return result.toString();
}

    /** Export the image of a [`PetriNet`] as a PNG file */
public String exportPetriNetImagePng(String net, String path) throws ErlangRpcException {
ErlTerm result = node.rpc(MODULE, "export_petri_net_image_png", List.of(new ErlBinary(net.getBytes(StandardCharsets.UTF_8)), new ErlBinary(path.getBytes(StandardCharsets.UTF_8))));
if (result instanceof ErlBinary eb) {
return new String(eb.data(), StandardCharsets.UTF_8);
}
return result.toString();
}

    /** Export the image of a [`PetriNet`] as a SVG file */
public String exportPetriNetImageSvg(String net, String path) throws ErlangRpcException {
ErlTerm result = node.rpc(MODULE, "export_petri_net_image_svg", List.of(new ErlBinary(net.getBytes(StandardCharsets.UTF_8)), new ErlBinary(path.getBytes(StandardCharsets.UTF_8))));
if (result instanceof ErlBinary eb) {
return new String(eb.data(), StandardCharsets.UTF_8);
}
return result.toString();
}

    /** Flatten an OCEL on a specific object type, resulting in a case-centric Event Log */
public String flattenOcelOn(String ocel, String objectType) throws ErlangRpcException {
ErlTerm result = node.rpc(MODULE, "flatten_ocel_on", List.of(new ErlBinary(ocel.getBytes(StandardCharsets.UTF_8)), new ErlBinary(objectType.getBytes(StandardCharsets.UTF_8))));
if (result instanceof ErlBinary eb) {
return new String(eb.data(), StandardCharsets.UTF_8);
}
return result.toString();
}

    /** Convert an [`EventLog`] into an [`EventLogActivityProjection`] */
public String logToActivityProjection(String log) throws ErlangRpcException {
ErlTerm result = node.rpc(MODULE, "log_to_activity_projection", List.of(new ErlBinary(log.getBytes(StandardCharsets.UTF_8))));
if (result instanceof ErlBinary eb) {
return new String(eb.data(), StandardCharsets.UTF_8);
}
return result.toString();
}

    /** Add an E2O (event-to-object) relationship with the specified qualifier */
public String locelAddE2o(String ocel, long event, long object, String qualifier) throws ErlangRpcException {
ErlTerm result = node.rpc(MODULE, "locel_add_e2o", List.of(new ErlBinary(ocel.getBytes(StandardCharsets.UTF_8)), new ErlInteger(event), new ErlInteger(object), new ErlBinary(qualifier.getBytes(StandardCharsets.UTF_8))));
if (result instanceof ErlBinary eb) {
return new String(eb.data(), StandardCharsets.UTF_8);
}
return result.toString();
}

    /** Add a new event to a [`SlimLinkedOCEL`] */
public String locelAddEvent(String ocel, String eventType, String time, String id, String attributes, String relationships) throws ErlangRpcException {
ErlTerm result = node.rpc(MODULE, "locel_add_event", List.of(new ErlBinary(ocel.getBytes(StandardCharsets.UTF_8)), new ErlBinary(eventType.getBytes(StandardCharsets.UTF_8)), new ErlBinary(time.getBytes(StandardCharsets.UTF_8)), new ErlBinary(id.getBytes(StandardCharsets.UTF_8)), new ErlBinary(attributes.getBytes(StandardCharsets.UTF_8)), new ErlBinary(relationships.getBytes(StandardCharsets.UTF_8))));
if (result instanceof ErlBinary eb) {
return new String(eb.data(), StandardCharsets.UTF_8);
}
return result.toString();
}

    /** Add a new event type to a [`SlimLinkedOCEL`] */
public String locelAddEventType(String ocel, String eventType, String attributes) throws ErlangRpcException {
ErlTerm result = node.rpc(MODULE, "locel_add_event_type", List.of(new ErlBinary(ocel.getBytes(StandardCharsets.UTF_8)), new ErlBinary(eventType.getBytes(StandardCharsets.UTF_8)), new ErlBinary(attributes.getBytes(StandardCharsets.UTF_8))));
if (result instanceof ErlBinary eb) {
return new String(eb.data(), StandardCharsets.UTF_8);
}
return result.toString();
}

    /** Add an O2O (object-to-object) relationship with the specified qualifier */
public String locelAddO2o(String ocel, long fromObj, long toObj, String qualifier) throws ErlangRpcException {
ErlTerm result = node.rpc(MODULE, "locel_add_o2o", List.of(new ErlBinary(ocel.getBytes(StandardCharsets.UTF_8)), new ErlInteger(fromObj), new ErlInteger(toObj), new ErlBinary(qualifier.getBytes(StandardCharsets.UTF_8))));
if (result instanceof ErlBinary eb) {
return new String(eb.data(), StandardCharsets.UTF_8);
}
return result.toString();
}

    /** Add a new object to a [`SlimLinkedOCEL`] */
public String locelAddObject(String ocel, String objectType, String id, String attributes, String relationships) throws ErlangRpcException {
ErlTerm result = node.rpc(MODULE, "locel_add_object", List.of(new ErlBinary(ocel.getBytes(StandardCharsets.UTF_8)), new ErlBinary(objectType.getBytes(StandardCharsets.UTF_8)), new ErlBinary(id.getBytes(StandardCharsets.UTF_8)), new ErlBinary(attributes.getBytes(StandardCharsets.UTF_8)), new ErlBinary(relationships.getBytes(StandardCharsets.UTF_8))));
if (result instanceof ErlBinary eb) {
return new String(eb.data(), StandardCharsets.UTF_8);
}
return result.toString();
}

    /** Add a new object type to a [`SlimLinkedOCEL`] */
public String locelAddObjectType(String ocel, String objectType, String attributes) throws ErlangRpcException {
ErlTerm result = node.rpc(MODULE, "locel_add_object_type", List.of(new ErlBinary(ocel.getBytes(StandardCharsets.UTF_8)), new ErlBinary(objectType.getBytes(StandardCharsets.UTF_8)), new ErlBinary(attributes.getBytes(StandardCharsets.UTF_8))));
if (result instanceof ErlBinary eb) {
return new String(eb.data(), StandardCharsets.UTF_8);
}
return result.toString();
}

    /** Reconstruct a full [`OCEL`] from a [`SlimLinkedOCEL`] */
public String locelConstructOcel(String ocel) throws ErlangRpcException {
ErlTerm result = node.rpc(MODULE, "locel_construct_ocel", List.of(new ErlBinary(ocel.getBytes(StandardCharsets.UTF_8))));
if (result instanceof ErlBinary eb) {
return new String(eb.data(), StandardCharsets.UTF_8);
}
return result.toString();
}

    /** Remove the E2O relationship between the given event and object */
public String locelDeleteE2o(String ocel, long event, long object) throws ErlangRpcException {
ErlTerm result = node.rpc(MODULE, "locel_delete_e2o", List.of(new ErlBinary(ocel.getBytes(StandardCharsets.UTF_8)), new ErlInteger(event), new ErlInteger(object)));
if (result instanceof ErlBinary eb) {
return new String(eb.data(), StandardCharsets.UTF_8);
}
return result.toString();
}

    /** Remove the O2O relationship between the given objects */
public String locelDeleteO2o(String ocel, long fromObj, long toObj) throws ErlangRpcException {
ErlTerm result = node.rpc(MODULE, "locel_delete_o2o", List.of(new ErlBinary(ocel.getBytes(StandardCharsets.UTF_8)), new ErlInteger(fromObj), new ErlInteger(toObj)));
if (result instanceof ErlBinary eb) {
return new String(eb.data(), StandardCharsets.UTF_8);
}
return result.toString();
}

    /** Get the E2O (event-to-object) relationships of an event as (qualifier, `object_index`) pairs */
public String locelGetE2o(String ocel, long ev) throws ErlangRpcException {
ErlTerm result = node.rpc(MODULE, "locel_get_e2o", List.of(new ErlBinary(ocel.getBytes(StandardCharsets.UTF_8)), new ErlInteger(ev)));
if (result instanceof ErlBinary eb) {
return new String(eb.data(), StandardCharsets.UTF_8);
}
return result.toString();
}

    /** Get the reverse E2O relationships of an object as (qualifier, `event_index`) pairs */
public String locelGetE2oRev(String ocel, long ob) throws ErlangRpcException {
ErlTerm result = node.rpc(MODULE, "locel_get_e2o_rev", List.of(new ErlBinary(ocel.getBytes(StandardCharsets.UTF_8)), new ErlInteger(ob)));
if (result instanceof ErlBinary eb) {
return new String(eb.data(), StandardCharsets.UTF_8);
}
return result.toString();
}

    /** Get the value of a specific event attribute by name */
public String locelGetEvAttrVal(String ocel, long ev, String attrName) throws ErlangRpcException {
ErlTerm result = node.rpc(MODULE, "locel_get_ev_attr_val", List.of(new ErlBinary(ocel.getBytes(StandardCharsets.UTF_8)), new ErlInteger(ev), new ErlBinary(attrName.getBytes(StandardCharsets.UTF_8))));
if (result instanceof ErlBinary eb) {
return new String(eb.data(), StandardCharsets.UTF_8);
}
return result.toString();
}

    /** Get an event index by its ID */
public String locelGetEvById(String ocel, String evId) throws ErlangRpcException {
ErlTerm result = node.rpc(MODULE, "locel_get_ev_by_id", List.of(new ErlBinary(ocel.getBytes(StandardCharsets.UTF_8)), new ErlBinary(evId.getBytes(StandardCharsets.UTF_8))));
if (result instanceof ErlBinary eb) {
return new String(eb.data(), StandardCharsets.UTF_8);
}
return result.toString();
}

    /** Get the ID of an event */
public String locelGetEvId(String ocel, long ev) throws ErlangRpcException {
ErlTerm result = node.rpc(MODULE, "locel_get_ev_id", List.of(new ErlBinary(ocel.getBytes(StandardCharsets.UTF_8)), new ErlInteger(ev)));
if (result instanceof ErlBinary eb) {
return new String(eb.data(), StandardCharsets.UTF_8);
}
return result.toString();
}

    /** Get the timestamp of an event */
public String locelGetEvTime(String ocel, long ev) throws ErlangRpcException {
ErlTerm result = node.rpc(MODULE, "locel_get_ev_time", List.of(new ErlBinary(ocel.getBytes(StandardCharsets.UTF_8)), new ErlInteger(ev)));
if (result instanceof ErlBinary eb) {
return new String(eb.data(), StandardCharsets.UTF_8);
}
return result.toString();
}

    /** Get the event type specification for a given type name */
public String locelGetEvType(String ocel, String evType) throws ErlangRpcException {
ErlTerm result = node.rpc(MODULE, "locel_get_ev_type", List.of(new ErlBinary(ocel.getBytes(StandardCharsets.UTF_8)), new ErlBinary(evType.getBytes(StandardCharsets.UTF_8))));
if (result instanceof ErlBinary eb) {
return new String(eb.data(), StandardCharsets.UTF_8);
}
return result.toString();
}

    /** Get the event type (activity) of an event */
public String locelGetEvTypeOf(String ocel, long ev) throws ErlangRpcException {
ErlTerm result = node.rpc(MODULE, "locel_get_ev_type_of", List.of(new ErlBinary(ocel.getBytes(StandardCharsets.UTF_8)), new ErlInteger(ev)));
if (result instanceof ErlBinary eb) {
return new String(eb.data(), StandardCharsets.UTF_8);
}
return result.toString();
}

    /** Get all event type names */
public String locelGetEvTypes(String ocel) throws ErlangRpcException {
ErlTerm result = node.rpc(MODULE, "locel_get_ev_types", List.of(new ErlBinary(ocel.getBytes(StandardCharsets.UTF_8))));
if (result instanceof ErlBinary eb) {
return new String(eb.data(), StandardCharsets.UTF_8);
}
return result.toString();
}

    /** Get all event indices of a given event type */
public String locelGetEvsOfType(String ocel, String evType) throws ErlangRpcException {
ErlTerm result = node.rpc(MODULE, "locel_get_evs_of_type", List.of(new ErlBinary(ocel.getBytes(StandardCharsets.UTF_8)), new ErlBinary(evType.getBytes(StandardCharsets.UTF_8))));
if (result instanceof ErlBinary eb) {
return new String(eb.data(), StandardCharsets.UTF_8);
}
return result.toString();
}

    /** Get the full materialized event (with expanded type, attributes, and relationships) */
public String locelGetFullEv(String ocel, long ev) throws ErlangRpcException {
ErlTerm result = node.rpc(MODULE, "locel_get_full_ev", List.of(new ErlBinary(ocel.getBytes(StandardCharsets.UTF_8)), new ErlInteger(ev)));
if (result instanceof ErlBinary eb) {
return new String(eb.data(), StandardCharsets.UTF_8);
}
return result.toString();
}

    /** Get the full materialized object (with expanded type, attributes, and relationships) */
public String locelGetFullOb(String ocel, long ob) throws ErlangRpcException {
ErlTerm result = node.rpc(MODULE, "locel_get_full_ob", List.of(new ErlBinary(ocel.getBytes(StandardCharsets.UTF_8)), new ErlInteger(ob)));
if (result instanceof ErlBinary eb) {
return new String(eb.data(), StandardCharsets.UTF_8);
}
return result.toString();
}

    /** Get the O2O (object-to-object) relationships of an object as (qualifier, `object_index`) pairs */
public String locelGetO2o(String ocel, long ob) throws ErlangRpcException {
ErlTerm result = node.rpc(MODULE, "locel_get_o2o", List.of(new ErlBinary(ocel.getBytes(StandardCharsets.UTF_8)), new ErlInteger(ob)));
if (result instanceof ErlBinary eb) {
return new String(eb.data(), StandardCharsets.UTF_8);
}
return result.toString();
}

    /** Get the reverse O2O relationships of an object as (qualifier, `object_index`) pairs */
public String locelGetO2oRev(String ocel, long ob) throws ErlangRpcException {
ErlTerm result = node.rpc(MODULE, "locel_get_o2o_rev", List.of(new ErlBinary(ocel.getBytes(StandardCharsets.UTF_8)), new ErlInteger(ob)));
if (result instanceof ErlBinary eb) {
return new String(eb.data(), StandardCharsets.UTF_8);
}
return result.toString();
}

    /** Get all values (with timestamps) of a specific object attribute by name */
public String locelGetObAttrVals(String ocel, long ob, String attrName) throws ErlangRpcException {
ErlTerm result = node.rpc(MODULE, "locel_get_ob_attr_vals", List.of(new ErlBinary(ocel.getBytes(StandardCharsets.UTF_8)), new ErlInteger(ob), new ErlBinary(attrName.getBytes(StandardCharsets.UTF_8))));
if (result instanceof ErlBinary eb) {
return new String(eb.data(), StandardCharsets.UTF_8);
}
return result.toString();
}

    /** Get an object index by its ID */
public String locelGetObById(String ocel, String obId) throws ErlangRpcException {
ErlTerm result = node.rpc(MODULE, "locel_get_ob_by_id", List.of(new ErlBinary(ocel.getBytes(StandardCharsets.UTF_8)), new ErlBinary(obId.getBytes(StandardCharsets.UTF_8))));
if (result instanceof ErlBinary eb) {
return new String(eb.data(), StandardCharsets.UTF_8);
}
return result.toString();
}

    /** Get the ID of an object */
public String locelGetObId(String ocel, long ob) throws ErlangRpcException {
ErlTerm result = node.rpc(MODULE, "locel_get_ob_id", List.of(new ErlBinary(ocel.getBytes(StandardCharsets.UTF_8)), new ErlInteger(ob)));
if (result instanceof ErlBinary eb) {
return new String(eb.data(), StandardCharsets.UTF_8);
}
return result.toString();
}

    /** Get the object type specification for a given type name */
public String locelGetObType(String ocel, String obType) throws ErlangRpcException {
ErlTerm result = node.rpc(MODULE, "locel_get_ob_type", List.of(new ErlBinary(ocel.getBytes(StandardCharsets.UTF_8)), new ErlBinary(obType.getBytes(StandardCharsets.UTF_8))));
if (result instanceof ErlBinary eb) {
return new String(eb.data(), StandardCharsets.UTF_8);
}
return result.toString();
}

    /** Get the object type of an object */
public String locelGetObTypeOf(String ocel, long ob) throws ErlangRpcException {
ErlTerm result = node.rpc(MODULE, "locel_get_ob_type_of", List.of(new ErlBinary(ocel.getBytes(StandardCharsets.UTF_8)), new ErlInteger(ob)));
if (result instanceof ErlBinary eb) {
return new String(eb.data(), StandardCharsets.UTF_8);
}
return result.toString();
}

    /** Get all object type names */
public String locelGetObTypes(String ocel) throws ErlangRpcException {
ErlTerm result = node.rpc(MODULE, "locel_get_ob_types", List.of(new ErlBinary(ocel.getBytes(StandardCharsets.UTF_8))));
if (result instanceof ErlBinary eb) {
return new String(eb.data(), StandardCharsets.UTF_8);
}
return result.toString();
}

    /** Get all object indices of a given object type */
public String locelGetObsOfType(String ocel, String obType) throws ErlangRpcException {
ErlTerm result = node.rpc(MODULE, "locel_get_obs_of_type", List.of(new ErlBinary(ocel.getBytes(StandardCharsets.UTF_8)), new ErlBinary(obType.getBytes(StandardCharsets.UTF_8))));
if (result instanceof ErlBinary eb) {
return new String(eb.data(), StandardCharsets.UTF_8);
}
return result.toString();
}

    /** Create a new empty [`SlimLinkedOCEL`] */
public String locelNew() throws ErlangRpcException {
ErlTerm result = node.rpc(MODULE, "locel_new", List.of());
if (result instanceof ErlBinary eb) {
return new String(eb.data(), StandardCharsets.UTF_8);
}
return result.toString();
}

    /** Convert an [`OCEL`] to an [`IndexLinkedOCEL`] */
public String indexLinkOcel(String ocel) throws ErlangRpcException {
ErlTerm result = node.rpc(MODULE, "index_link_ocel", List.of(new ErlBinary(ocel.getBytes(StandardCharsets.UTF_8))));
if (result instanceof ErlBinary eb) {
return new String(eb.data(), StandardCharsets.UTF_8);
}
return result.toString();
}

    /** Get the number of events in an [`OCEL`] */
public String numEvents(String ocel) throws ErlangRpcException {
ErlTerm result = node.rpc(MODULE, "num_events", List.of(new ErlBinary(ocel.getBytes(StandardCharsets.UTF_8))));
if (result instanceof ErlBinary eb) {
return new String(eb.data(), StandardCharsets.UTF_8);
}
return result.toString();
}

    /** Get the number of objects in an [`OCEL`] */
public String numObjects(String ocel) throws ErlangRpcException {
ErlTerm result = node.rpc(MODULE, "num_objects", List.of(new ErlBinary(ocel.getBytes(StandardCharsets.UTF_8))));
if (result instanceof ErlBinary eb) {
return new String(eb.data(), StandardCharsets.UTF_8);
}
return result.toString();
}

    /** Compute statistics on object/event types in the OCEL */
public String ocelTypeStats(String ocel) throws ErlangRpcException {
ErlTerm result = node.rpc(MODULE, "ocel_type_stats", List.of(new ErlBinary(ocel.getBytes(StandardCharsets.UTF_8))));
if (result instanceof ErlBinary eb) {
return new String(eb.data(), StandardCharsets.UTF_8);
}
return result.toString();
}

    /** Convert an [`OCEL`] to an [`SlimLinkedOCEL`] */
public String slimLinkOcel(String ocel) throws ErlangRpcException {
ErlTerm result = node.rpc(MODULE, "slim_link_ocel", List.of(new ErlBinary(ocel.getBytes(StandardCharsets.UTF_8))));
if (result instanceof ErlBinary eb) {
return new String(eb.data(), StandardCharsets.UTF_8);
}
return result.toString();
}

    /** This is a test function. */
public String testSomeInputs(String s, int n, int i, double f, boolean b) throws ErlangRpcException {
ErlTerm result = node.rpc(MODULE, "test_some_inputs", List.of(new ErlBinary(s.getBytes(StandardCharsets.UTF_8)), new ErlInteger((long) n), new ErlInteger((long) i), new ErlFloat(f), new ErlAtom(b ? "true" : "false")));
if (result instanceof ErlBinary eb) {
return new String(eb.data(), StandardCharsets.UTF_8);
}
return result.toString();
}

}
