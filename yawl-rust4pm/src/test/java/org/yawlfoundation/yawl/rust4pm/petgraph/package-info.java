/**
 * Tests for PetriNetGraph JNI bindings.
 *
 * Covers:
 * - Basic graph operations (create, add node, add edge)
 * - Graph queries (node count, edge count, path existence)
 * - Neighbor traversal and reachability
 * - JSON serialization
 * - Resource cleanup
 */
package org.yawlfoundation.yawl.rust4pm.petgraph;
