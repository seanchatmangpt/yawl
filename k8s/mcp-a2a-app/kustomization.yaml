# YAWL MCP-A2A Application Kustomize Root
# Deploy to default environment: kubectl apply -k .
# Deploy to specific environment: kubectl apply -k overlays/prod
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - base
