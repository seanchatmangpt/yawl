# Ordering Agent - Kubernetes Deployment
# Autonomous agent for procurement, purchase orders, approvals
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ordering-agent
  labels:
    app: ordering-agent
    domain: orderfulfillment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ordering-agent
  template:
    metadata:
      labels:
        app: ordering-agent
        domain: orderfulfillment
    spec:
      containers:
        - name: agent
          image: yawl/party-agent:latest
          env:
            - name: AGENT_CAPABILITY
              value: "Ordering: procurement, purchase orders, approvals, order lifecycle"
            - name: AGENT_PORT
              value: "8091"
            - name: YAWL_ENGINE_URL
              valueFrom:
                configMapKeyRef:
                  name: yawl-config
                  key: engine_url
            - name: YAWL_USERNAME
              valueFrom:
                secretKeyRef:
                  name: yawl-credentials
                  key: username
            - name: YAWL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: yawl-credentials
                  key: password
            - name: ZAI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: zai-secrets
                  key: api_key
          ports:
            - containerPort: 8091
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 8091
            initialDelaySeconds: 30
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: ordering-agent
spec:
  selector:
    app: ordering-agent
  ports:
    - port: 8091
      targetPort: 8091
  type: ClusterIP
