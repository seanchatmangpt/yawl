/**
 * Generated by ggen from YAWL Extended Workflow Patterns Ontology v6.0.0
 * Source: extended-patterns.ttl
 * Generated: {{ generated.timestamp }}
 *
 * DO NOT EDIT - Changes will be overwritten on next generation
 */
package {{ java_package }}.patterns;

import org.yawlfoundation.yawl.generated.annotation.Generated;
import java.lang.annotation.*;

/**
 * Workflow Pattern Annotation: {{ pattern.name }}
 *
 * <p>
 * <strong>Pattern ID:</strong> {{ pattern.id }}<br>
 * <strong>Pattern Category:</strong> {{ pattern.category }}
 * </p>
 *
 * <h3>Description</h3>
 * <p>
 * {{ pattern.definition }}
 * </p>
 *
 * {% if pattern.formalism -%}
 * <h3>Formal Specification</h3>
 * <pre><code>
 * {{ pattern.formalism }}
 * </code></pre>
 * {% endif -%}
 *
 * {% if pattern.use_case -%}
 * <h3>Use Cases</h3>
 * <p>
 * {{ pattern.use_case }}
 * </p>
 * {% endif -%}
 *
 * {% if pattern.related_patterns | length > 0 -%}
 * <h3>Related Patterns</h3>
 * <p>
 * This pattern is related to:
 * <ul>
 * {% for related in pattern.related_patterns -%}
 * <li><code>{{ related.name }}</code> ({{ related.id }})</li>
 * {% endfor -%}
 * </ul>
 * </p>
 * {% endif -%}
 *
 * <h3>Usage Example</h3>
 * <pre><code>
 * @{{ pattern.name | pascalcase }}(
 *     id = "{{ pattern.id }}",
 *     description = "{{ pattern.definition | truncate(length=50) }}..."
 * )
 * public class MyWorkflow extends WorkflowDefinition {
 *     // Workflow implementation following {{ pattern.name }}
 * }
 * </code></pre>
 *
 * @author YAWL Foundation
 * @version {{ generated.version }}
 * @since 6.0.0
 * @see <a href="https://www.yawlfoundation.org/patterns/">YAWL Workflow Patterns</a>
 */
@Target({
    ElementType.TYPE,
    ElementType.METHOD,
    ElementType.FIELD
})
@Retention(RetentionPolicy.RUNTIME)
@Documented
@Generated(
    value = "org.yawlfoundation.ggen.CodeGenerator",
    date = "{{ generated.timestamp }}",
    comments = "Generated from workflow pattern: {{ pattern.uri }}"
)
public @interface {{ pattern.name | pascalcase }} {

    /**
     * Pattern ID in the YAWL control-flow patterns taxonomy
     *
     * @return the pattern identifier
     */
    String id() default "{{ pattern.id }}";

    /**
     * Pattern name
     *
     * @return the human-readable pattern name
     */
    String name() default "{{ pattern.name }}";

    /**
     * Detailed description of the pattern
     *
     * @return the pattern description
     */
    String description() default """
        {{ pattern.definition }}
        """;

    /**
     * Pattern category
     *
     * @return the category (e.g., DistributedTransaction, EventDriven, Resilience)
     */
    String category() default "{{ pattern.category }}";

    /**
     * Formal specification of the pattern
     *
     * @return mathematical or algorithmic specification
     */
    String formalism() default """
        {{ pattern.formalism | default(value="See pattern definition") }}
        """;

    /**
     * Common use cases for this pattern
     *
     * @return array of example use cases
     */
    String[] useCases() default {
{% for use_case in pattern.use_cases -%}
        "{{ use_case }}"{{ if not loop.last }},{{ endif }}
{% endfor -%}
    };

    /**
     * Related workflow patterns
     *
     * @return array of related pattern names
     */
    String[] relatedPatterns() default {
{% for related in pattern.related_patterns -%}
        "{{ related.name }}"{{ if not loop.last }},{{ endif }}
{% endfor -%}
    };

    /**
     * Whether this pattern is applicable to distributed/microservices architectures
     *
     * @return true if pattern is suitable for distributed systems
     */
    boolean isDistributed() default {{ pattern.is_distributed | default(value="false") }};

    /**
     * Whether this pattern provides fault tolerance/resilience capabilities
     *
     * @return true if pattern includes resilience patterns
     */
    boolean isResilient() default {{ pattern.is_resilient | default(value="false") }};

    /**
     * Whether this pattern supports temporal constraints
     *
     * @return true if pattern includes time-based control
     */
    boolean isTemporal() default {{ pattern.is_temporal | default(value="false") }};

    /**
     * Whether this pattern supports event-driven execution
     *
     * @return true if pattern is event-driven
     */
    boolean isEventDriven() default {{ pattern.is_event_driven | default(value="false") }};

    /**
     * Whether this pattern supports AI/ML integration
     *
     * @return true if pattern is compatible with AI models
     */
    boolean hasAISupport() default {{ pattern.has_ai_support | default(value="false") }};

    /**
     * Minimum YAWL version required for this pattern
     *
     * @return semantic version string
     */
    String minVersion() default "{{ pattern.min_version | default(value="4.0.0") }}";

    /**
     * Whether this pattern is experimental/draft status
     *
     * @return true if pattern is still evolving
     */
    boolean experimental() default {{ pattern.experimental | default(value="false") }};

    /**
     * Author organization
     *
     * @return the organization that defined/extended this pattern
     */
    String author() default "{{ pattern.author | default(value="YAWL Foundation") }}";

    /**
     * Citation or reference URL
     *
     * @return URL to pattern specification or paper
     */
    String reference() default "{{ pattern.reference | default(value="https://www.yawlfoundation.org") }}";

    /**
     * Configuration example in YAWL XML format
     *
     * @return example XML snippet
     */
    String exampleXml() default "";

    /**
     * Configuration example in YAWL RDF/Turtle format
     *
     * @return example RDF snippet
     */
    String exampleRdf() default "";
}
