-Xms2g
-Xmx8g
-XX:MetaspaceSize=256m
-XX:MaxMetaspaceSize=512m

# Java 25 Compact Object Headers - 5-10% throughput, 10-20% memory
-XX:+UseCompactObjectHeaders

# ZGC - Ultra-low latency garbage collection (<1ms pause times)
-XX:+UseZGC
-XX:+UseStringDeduplication
-XX:InitiatingHeapOccupancyPercent=35

# Tiered compilation for balanced performance
-XX:+TieredCompilation
-XX:TieredStopAtLevel=4
-XX:CompileThreshold=10000

# Parallelism tuning
-Djava.util.concurrent.ForkJoinPool.common.parallelism=24

# Virtual thread scheduler tuning (Java 25)
-Djdk.virtualThreadScheduler.parallelism=auto

# Encoding and build cache
-Dfile.encoding=UTF-8
--enable-preview
-Dmaven.build.cache.enabled=true
