# YAWL v6.0.0-GA - JVM Configuration (Baseline Compilation)
# Memory configuration
-Xms4g -Xmx8g

# Garbage collection: ZGC with generational mode (Java 21+)
-XX:+UseZGC
-XX:+ZGenerational

# Memory efficiency (Java 25+)
-XX:+UseCompactObjectHeaders
-XX:+UseStringDeduplication

# Metaspace for large codebases
-XX:MetaspaceSize=256m
-XX:MaxMetaspaceSize=512m

# JIT compilation (baseline)
-XX:+TieredCompilation
-XX:TieredStopAtLevel=4
-XX:CompileThreshold=10000

# Parallelism
-Djava.util.concurrent.ForkJoinPool.common.parallelism=15
-Djdk.virtualThreadScheduler.parallelism=16

# Virtual thread pinning detection
-Djdk.tracePinnedThreads=short

# Proxy settings (override broken JAVA_TOOL_OPTIONS)
-Dhttp.proxyHost=21.0.0.59
-Dhttp.proxyPort=15004
-Dhttps.proxyHost=21.0.0.59
-Dhttps.proxyPort=15004
-Dhttp.nonProxyHosts=localhost|127.0.0.1|169.254.169.254|metadata.google.internal|*.svc.cluster.local|*.local|*.googleapis.com|*.google.com

# Encoding
-Dfile.encoding=UTF-8

# Java 25 preview features
--enable-preview

# Maven build cache
-Dmaven.build.cache.enabled=true
-Dmaven.artifact.threads=8
