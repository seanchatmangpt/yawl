<?xml version="1.0" encoding="UTF-8"?>
<extensions>
    <!-- Maven 4 build cache extension for Java 25.
         Provides multi-build incremental caching with XX hash algorithm.

         DISABLED: The maven-build-cache-extension 1.2.1 is not fully compatible
         with Maven 4.0.0-rc-5. Will re-enable when extension is updated.

         For Maven 3.9.x, this extension works with local caching.
    -->
    <!-- extension>
        <groupId>org.apache.maven.extensions</groupId>
        <artifactId>maven-build-cache-extension</artifactId>
        <version>1.2.1</version>
    </extension -->
</extensions>
