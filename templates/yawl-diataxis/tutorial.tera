{#
 Diataxis Tutorial Template
 Generates learning-oriented documentation for YAWL packages.

 Context Variables:
   - package_name: Full package name (e.g., org.yawlfoundation.yawl.engine.core)
   - package_short: Short name (e.g., engine.core)
   - package_description: Description from package-info.java
   - key_classes: Array of important classes in the package
   - subpackages: Array of subpackage names
   - since_version: Version when package was introduced
   - author: Package author
   - version: YAWL version
 #}
# {{ package_short | replace(from=".", to=" ") | title }} Package Tutorial

**Audience**: Developers new to the {{ package_short }} package
**Time**: ~15 minutes
**Prerequisites**: Java 25+, Maven 3.9+

## What You'll Learn

By the end of this tutorial, you will:
- Understand the purpose of the {{ package_short }} package
- Know how to use key classes{% if key_classes %}: {{ key_classes | join(sep=", ") }}{% endif %}
- Be able to integrate with other YAWL components

## Overview

{{ package_description | default(value="This package is part of the YAWL workflow engine.", safe=true) }}

{% if subpackages %}
## Package Structure

This package contains the following subpackages:

| Subpackage | Purpose |
|------------|---------|
{% for subpkg in subpackages %}
| `{{ subpkg }}` | TODO: Add description |
{% endfor %}

{% endif %}
## Step 1: Setup

{% if package_name contains "engine" and not package_name contains "stateless" %}
```java
// Initialize the YAWL engine for stateful execution
import org.yawlfoundation.yawl.engine.YEngine;

YEngine engine = YEngine.getInstance();
```
{% elif package_name contains "stateless" %}
```java
// Initialize the stateless engine
import org.yawlfoundation.yawl.stateless.engine.YStatelessEngine;

YStatelessEngine engine = new YStatelessEngine();
```
{% elif package_name contains "elements" %}
```java
// Create a workflow specification
import org.yawlfoundation.yawl.elements.YSpecification;
import org.yawlfoundation.yawl.elements.YNet;

YSpecification spec = new YSpecification("my-workflow");
YNet rootNet = new YNet("root", spec);
```
{% elif package_name contains "integration.mcp" %}
```java
// Initialize MCP server
import org.yawlfoundation.yawl.integration.mcp.YawlMcpServer;

YawlMcpServer server = new YawlMcpServer();
server.start();
```
{% elif package_name contains "integration.a2a" %}
```java
// Initialize A2A protocol handler
import org.yawlfoundation.yawl.integration.a2a.YawlA2AServer;

YawlA2AServer server = new YawlA2AServer();
```
{% elif package_name contains "resilience" %}
```java
// Configure resilience features
import org.yawlfoundation.yawl.resilience.ResilienceManager;

ResilienceManager manager = ResilienceManager.getInstance();
```
{% elif package_name contains "security" %}
```java
// Configure security
import org.yawlfoundation.yawl.security.SecurityManager;

SecurityManager security = SecurityManager.getInstance();
```
{% else %}
```java
// Import the package classes
import {{ package_name }}.*;
```
{% endif %}

## Step 2: Basic Usage

{% if key_classes %}
{% for class in key_classes %}
### Using {{ class }}

```java
// TODO: Add usage example for {{ class }}
{{ class }} instance = new {{ class }}();
```
{% endfor %}
{% else %}
### Basic Usage

```java
// TODO: Add usage example
```
{% endif %}

## Step 3: Common Patterns

```java
// TODO: Add common usage patterns specific to this package
```

## Step 4: Integration with Other Components

This package integrates with:

{% if package_name contains "engine" %}
- **elements** - For specification parsing
- **stateless** - For stateless execution mode
- **resourcing** - For work item allocation
{% elif package_name contains "elements" %}
- **engine** - For specification execution
- **schema** - For XML validation
{% elif package_name contains "integration" %}
- **engine** - For workflow execution
- **resilience** - For fault tolerance
{% else %}
- Other YAWL packages (TODO: specify)
{% endif %}

## Common Pitfalls

| Issue | Solution |
|-------|----------|
| TODO | Add common issues and solutions |

## Next Steps

- Read the [How-To Guide](../how-to/) for task-oriented guides
- Check the [API Reference](../reference/api.md) for detailed API docs
- Understand the [Architecture](../explanation/) for design decisions

## See Also

{% if subpackages %}
- Subpackages:
{% for subpkg in subpackages %}
  - [{{ subpkg }}](../{{ subpkg | replace(from=".", to="/") }}/tutorials/)
{% endfor %}
{% endif %}

---
*Generated by ggen from package-info.java*
*Package: `{{ package_name }}`*
*Since: {{ since_version | default(value="6.0.0-GA") }}*
